import { useTreeItem } from "@mui/lab";
import { Typography } from "@mui/material";
import { useState } from "react";
import { Link } from "react-router-dom";
import { Tree } from "../redux/tree";

interface ISummary {
    isSummary: Boolean
}

const Tree = ({ isSummary }: ISummary) => {
    
}

export default Tree